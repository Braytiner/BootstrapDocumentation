
@{
    ViewData["Title"] = "Alinhamento horizontal";
}

<h5>justify-content-start</h5>
<div class="container cor-fundo-container">
    <div class="row cor-fundo-linha justify-content-start">
        <div class="col-4 cor-fundo-coluna">Primeiro item</div>
        <div class="col-4 cor-fundo-coluna">Segundo item</div>
    </div>
</div>

<h5>justify-content-center</h5>
<div class="container cor-fundo-container">
    <div class="row cor-fundo-linha justify-content-center">
        <div class="col-4 cor-fundo-coluna">Primeiro item</div>
        <div class="col-4 cor-fundo-coluna">Segundo item</div>
    </div>
</div>

<h5>justify-content-end</h5>
<div class="container cor-fundo-container">
    <div class="row cor-fundo-linha justify-content-end">
        <div class="col-4 cor-fundo-coluna">Primeiro item</div>
        <div class="col-4 cor-fundo-coluna">Segundo item</div>
    </div>
</div>

<h5>justify-content-around</h5>
<div class="container cor-fundo-container">
    <div class="row cor-fundo-linha justify-content-around">
        <div class="col-4 cor-fundo-coluna">Primeiro item</div>
        <div class="col-4 cor-fundo-coluna">Segundo item</div>
    </div>
</div>

<h5>justify-content-between</h5>
<div class="container cor-fundo-container">
    <div class="row cor-fundo-linha justify-content-between">
        <div class="col-4 cor-fundo-coluna">Primeiro item</div>
        <div class="col-4 cor-fundo-coluna">Segundo item</div>
    </div>
</div>

<h5>no-gutters</h5>
<div class="container cor-fundo-container">        
    <div class="row cor-fundo-linha">
        <div class="col-4 cor-fundo-coluna">Primeiro item</div>
        <div class="col-4 cor-fundo-coluna">Segundo item</div>
    </div>
</div>

